name: Intune Policy Import & Assignment

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  id-token: write
  contents: write
  pull-requests: write

env:
  REPO_DIR: ${{ github.workspace }}
  STAGING_FOLDER: "${{ github.workspace }}/staging"
  CSV_PATH: "${{ github.workspace }}/staging/staging.csv"

jobs:

  # -------------------------
  # Import Policies
  # -------------------------
  import:
    runs-on: ubuntu-latest
    environment: GraphApi
    outputs:
      imported_policies: ${{ steps.import_policies.outputs.imported_policies }}
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          allow-no-subscriptions: true

      - name: Install Graph Modules
        shell: pwsh
        run: |
          Install-Module Microsoft.Graph.Authentication -Force -AllowClobber
          Install-Module Microsoft.Graph.Beta.DeviceManagement -Force -AllowClobber

      - name: Import Policies from Staging Folder
        id: import_policies
        shell: pwsh
        run: |
          $GraphTokenResponse = az account get-access-token --resource https://graph.microsoft.com
          $GraphToken = ($GraphTokenResponse | ConvertFrom-Json).accessToken
          $SecureToken = ConvertTo-SecureString $GraphToken -AsPlainText -Force
          Connect-MgGraph -AccessToken $SecureToken
          Import-Module -Name "${{ env.REPO_DIR }}" -Force
          $policyFiles = Get-ChildItem "${{ env.REPO_DIR }}/staging" -File -Filter *.json
          $policyMap = @{}

          foreach ($file in $policyFiles) {
              Write-Host "Importing policy: $($file.Name)"
              $policyId = Import-IntuneSettingsCatalogPolicy -folder "${{ env.REPO_DIR }}/staging" -fileName $file.Name
              $policyMap[$file.Name] = $policyId
          }

          # Clear staging folder
          Remove-Item -Path "${{ env.REPO_DIR }}/staging/" -File -Filter *.json -Force

          # Output mapping for downstream jobs
          $allIds = $policyMap.GetEnumerator() | ForEach-Object { "$($_.Key)=$($_.Value)" } -join ';'
          echo "imported_policies=$allIds" >> $GITHUB_OUTPUT

  # -------------------------
  # Assign Policies
  # -------------------------
  assign:
    needs: import
    runs-on: ubuntu-latest
    environment: GraphApi
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          allow-no-subscriptions: true

      - name: Install Graph Modules
        shell: pwsh
        run: |
          Install-Module Microsoft.Graph.Authentication -Force -AllowClobber
          Install-Module Microsoft.Graph.Beta.DeviceManagement -Force -AllowClobber

      - name: Assign Policies
        shell: pwsh
        run: |
          $GraphTokenResponse = az account get-access-token --resource https://graph.microsoft.com
          $GraphToken = ($GraphTokenResponse | ConvertFrom-Json).accessToken
          $SecureToken = ConvertTo-SecureString $GraphToken -AsPlainText -Force
          Connect-MgGraph -AccessToken $SecureToken

          # Parse imported policies
          $policyIdsOutput = "${{ needs.import.outputs.imported_policies }}"
          $policyMap = @{}
          foreach ($kv in $policyIdsOutput -split ';') {
              $parts = $kv -split '='
              $policyMap[$parts[0]] = $parts[1]
          }

          # Read CSV for assignments
          $rows = Import-Csv -Path "${{ env.CSV_PATH }}"
          foreach ($row in $rows) {
              $policyId = $policyMap[$row.FileName]
              if (-not $policyId) { continue }

              $assignBody = @{
                  assignments = @()
              }

              # Include groups
              if ($row.IncludeGroups) {
                  foreach ($g in $row.IncludeGroups -split ',') {
                      $target = @{
                          "@odata.type" = "#microsoft.graph.groupAssignmentTarget"
                          groupId = $g.Trim()
                      }
                      if ($row.FilterMode -eq "Include" -and $row.Filter) {
                          $filterObj = @{
                              "@odata.type" = "#microsoft.graph.deviceAndAppManagementAssignmentFilter"
                              id = (Invoke-MgGraphRequest -Method GET -Uri "https://graph.microsoft.com/beta/deviceManagement/assignmentFilters?`$filter=displayName eq '$($row.Filter)'").value[0].id
                              assignmentFilterType = (Invoke-MgGraphRequest -Method GET -Uri "https://graph.microsoft.com/beta/deviceManagement/assignmentFilters?`$filter=displayName eq '$($row.Filter)'").value[0].assignmentFilterType
                          }
                          $target.assignmentFilter = $filterObj
                      }
                      $assignBody.assignments += $target
                  }
              }

              # Exclude groups
              if ($row.ExcludeGroups) {
                  foreach ($g in $row.ExcludeGroups -split ',') {
                      $target = @{
                          "@odata.type" = "#microsoft.graph.exclusionGroupAssignmentTarget"
                          groupId = $g.Trim()
                      }
                      $assignBody.assignments += $target
                  }
              }

              if ($assignBody.assignments.Count -gt 0) {
                  Invoke-MgGraphRequest -Method POST `
                    -Uri "https://graph.microsoft.com/beta/deviceManagement/configurationPolicies/$policyId/assign" `
                    -Body ($assignBody | ConvertTo-Json -Depth 10)
                  Write-Host "Policy '$($row.FileName)' assigned successfully"
              }
          }
